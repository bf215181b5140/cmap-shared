import { ReactNode } from 'react';
export interface ReactProps {
    children?: ReactNode;
}
export declare enum ContentBoxWidth {
    None = "None",
    Third = "Third",
    Half = "Half",
    Full = "Full"
}
